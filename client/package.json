{
  "name": "sudharsan-showcase",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "serve": "^14.2.0"            // ✅ Added for serving production build
  },
  "scripts": {
    "start": "serve -s build",    // ✅ Updated for production deployment
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
